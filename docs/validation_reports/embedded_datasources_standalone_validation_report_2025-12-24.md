# 嵌入式数据源(独立) 验证分析报告

**执行时间**: 2025-12-24 23:15
**验证状态**: ⚠️ 有差异

## 一、执行摘要

| 验证类别     | 通过项 | 异常项 | 通过率 |
| :----------- | :----: | :----: | :----: |
| 核心属性验证 |   1    |   0    |  100%  |
| 血缘质量检测 |   6    |   1    |  85%   |
| 关联元素验证 |   4    |   0    |  100%  |
| 前端元素对齐 |   1    |   0    |  100%  |
| 逻辑闭环验证 |   1    |   0    |  100%  |

## 二、问题详情

### 2.1 [质量问题]: 孤儿独立嵌入式数据源 (无上游表关联)

**位置**: `血缘质量检测.未匹配血缘检测[关联方向='独立嵌入源->表']`

**状态**: 发现 5 个独立嵌入式数据源没有任何关联的物理表或嵌入表。这意味着这些数据源可能引用了已失效的连接，或者是尚未完全扫描到其组成结构的残余记录。

**验证SQL**:
```sql
SELECT id, name FROM datasources WHERE is_embedded = 1 AND source_published_datasource_id IS NULL AND NOT EXISTS (SELECT 1 FROM table_to_datasource WHERE datasource_id = datasources.id);
```

## 三、修复建议

### 3.1 [建议]: 深度扫描独立嵌入数据源连接

**修复方案**: 建议在同步过程中，对此类无表关联的独立嵌入源进行强制重连扫描，以确认其底层表结构是否真的为空还是同步丢失。

## 四、修复执行记录

无
